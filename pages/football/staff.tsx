import React, { ReactElement } from 'react'
import Layout from '../../components/layouts/Layout'

const Staff = () => {}

Staff.getLayout = (page: ReactElement) => <Layout title="Staff">{page}</Layout>

export default Staff
